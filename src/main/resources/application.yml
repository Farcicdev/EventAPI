spring:
  application:
    name: EventAPI

  datasource:
    url: jdbc:postgresql://localhost:5434/db_eventAPI
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true

  flyway:
    enabled: true